# Runtime — enough to serve the dashboard AND run the live simulation.
# Deliberately no torch: inference goes through policy_rl.onnx via onnxruntime
# (see agents/policy_runtime.py), which is what makes the simulation fit in a
# 512 MB instance alongside the web server.
fastapi
uvicorn
websockets
httpx
numpy
onnxruntime
redis
kafka-python

# Analysis / plotting
networkx
matplotlib

# Development only — training (train.py) and batch validation (simulate.py).
# Not needed to serve the dashboard or run the live simulation.
#   torch           train.py, agents/bc_trainer.py, agents/rl_trainer.py, simulate.py
#   pandas          data/loader.py, data/graph_builder.py
#   scikit-learn    data/persona_cluster.py
#   requests        simulate.py (calls the microservices over HTTP)
# torch
# pandas
# scikit-learn
# requests
